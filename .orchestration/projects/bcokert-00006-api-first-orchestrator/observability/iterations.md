# Iteration log
<!-- Format: ## YYYY-MM-DD HH:MM — [state] What changed. Why. -->

## 2026-04-27 — [design] Initial design doc written. Captures: server-as-deterministic-store (no LLM), Go + sqlite + filesystem, action-focused API with state/discovery GETs, single-folder install at ~/.orchestrator/, per-repo .orchestration.env, optimistic concurrency, server-managed queue, slice plan starting with status migration, dogfooding strategy. Bdon's framing throughout — graph-first, then actions, then storage/API.
